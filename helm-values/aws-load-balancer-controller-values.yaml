# Replace placeholders after Terraform outputs are available.
clusterName: kubernetes-platform-dev
region: us-east-1
vpcId: REPLACE_WITH_VPC_ID

serviceAccount:
  create: true
  name: aws-load-balancer-controller
  annotations:
    eks.amazonaws.com/role-arn: REPLACE_WITH_AWS_LOAD_BALANCER_CONTROLLER_ROLE_ARN
