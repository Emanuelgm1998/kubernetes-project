global:
  nodeSelector:
    role: system
  tolerations:
    - key: CriticalAddonsOnly
      operator: Equal
      value: "true"
      effect: NoSchedule

server:
  service:
    type: ClusterIP

configs:
  params:
    server.insecure: false
